# Precious AI — server-side environment configuration (optional).
# API keys can ALSO be entered via Admin -> API Settings (stored server-side only).
# Values here override nothing: DB-stored keys take precedence over env vars.

GROQ_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
OPENROUTER_API_KEY=

# Server
PORT=8000
